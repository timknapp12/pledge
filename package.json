{
  "name": "pledge",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "clean": "turbo run clean && rm -rf node_modules",
    "postinstall": "node scripts/colocate-rnfirebase.mjs"
  },
  "devDependencies": {
    "@expo/config-plugins": "^55.0.8",
    "assert": "^2.1.0",
    "stream-browserify": "^3.0.0",
    "turbo": "^2.3.0",
    "util": "^0.12.5"
  },
  "packageManager": "npm@10.2.0",
  "engines": {
    "node": ">=18"
  }
}
